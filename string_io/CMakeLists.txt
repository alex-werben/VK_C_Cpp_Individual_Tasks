cmake_minimum_required(VERSION 3.0.0)
project(string_io)

file(GLOB SOURCES *.c)
file(GLOB INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR})

add_library(${PROJECT_NAME} ${SOURCES})

set(STRING_IO_LIBRARIES ${PROJECT_NAME} PARENT_SCOPE)
set(STRING_IO_INCLUDE_DIRS ${INCLUDE_DIRS} PARENT_SCOPE)
